import java.io.File
import java.io.PrintWriter

fun main(){
    val fileName = "data.txt"
    //var file = File(fileName)
    //if (file.createNewFile()) println("$fileName se ha creado correctamente") else println("$fileName  ya existe")

    //val content = "Hello World. Welcome to Kotlin Tutorial by www.tutorialkart.com."
    //val writer = PrintWriter(fileName)
    //writer.append(content)
    //writer.close()

    //val content:String = " This is additional content added to the File."
    //File(fileName).appendText(content)

    //File(fileName).delete()
}